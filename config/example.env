# 这个文件只是参数示例，不会被 ktnet 自动读取。
# 不要把代理订阅、Tailscale auth key、API key 或设备密码写入仓库。

# 自动识别失败时才需要显式传入：
TAILNET_DOMAIN=tailxxxx.ts.net

# 如果 Tailnet 广播了额外子网，应同时：
# 1. configure 加 --accept-routes
# 2. 为每个网段重复加 --extra-route
EXTRA_ROUTE_1=192.168.50.0/24
